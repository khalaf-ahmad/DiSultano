// fetching current order from order builder
export const order = state => state.order_builder.order;

// fetching current page of orders in order builder
export const page = state => state.order_builder.page;